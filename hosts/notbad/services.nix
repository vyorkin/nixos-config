{
  services = {
    # Enable power management daemon. UPower numerates power
    # sources, maintains statistics and history data on them and
    # notifies about status changes
    upower.enable = true;
  };
}
